[project]
name = "imdb-card-generator"
version = "0.1.0"
description = "imdb card generator"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
  "python-dotenv>=1.2.2",
  "requests==2.31.0",
  "streamlit==1.35.0",
]
