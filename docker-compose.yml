services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: imdb-card-generator
    ports:
      - "8501:8501"
    env_file:
      - .env
    restart: unless-stopped
